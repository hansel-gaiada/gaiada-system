# Dependencies / build output (all components)
node_modules/
dist/
.next/
*.tsbuildinfo
*.log

# Secrets — real env files never leave the machine (.env.example is the template)
.env
.env.local
.env.tmp
.env.*.local
# Operator credential notes. Anything matching *.local.md is a working copy of real secrets:
# logins, API keys, reset runbooks. Never committed, never in a PR, never in CI logs.
*.local.md
CREDENTIALS.local.md
