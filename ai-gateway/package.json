{
  "name": "gaiada-ai-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gaiada AI Gateway (WS3) — the single AI-provider egress: key custody, provider chain + failover, fail-closed DLP, cost cap, audit",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
