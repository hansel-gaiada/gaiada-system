// webdesk/blocks/demo/astro.config.mjs — verification-only demo site. See package.json's
// description for why this exists and why it is not part of the published package.
import { defineConfig } from 'astro/config'

export default defineConfig({
  output: 'static',
})
