{
  "name": "gaiada-lab-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gaiada LMS lab runner (LMS L5) — executes a learner's submission in a capped, unprivileged, network-less container and returns a graded result. No ERP network path, no identity, no database.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
