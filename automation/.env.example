# Copy to .env
# n8n 2.x has no basic-auth — you create the admin/owner account in the UI on first visit.

# --- n8n runtime ---
# Encryption key: pin it so saved credentials survive a volume reset. Generate one with:
#   openssl rand -hex 32
N8N_ENCRYPTION_KEY=
# Public URL n8n uses to register webhooks (the event bridge posts to <this>/webhook/ev/...).
# Leave as-is for local; set to the tunneled/public origin when reachable from outside.
N8N_WEBHOOK_URL=http://localhost:5678/
TZ=Asia/Singapore
# Postgres backing (gaiada_n8n on the core instance) — MUST match KEYCLOAK/N8N provisioning.
# N8N_DB_PASSWORD must equal the core stack's N8N_DB_PASSWORD. N8N_DB_HOST defaults to the compose
# service `postgres`; when running this stack STANDALONE (not on the VPS network) set it to
# host.docker.internal (or the core DB host) so n8n can reach gaiada_n8n.
N8N_DB_PASSWORD=
N8N_DB_HOST=host.docker.internal

# --- Workflow variables (imported flows read these as $env.<NAME>) ---
# Hub service token + tenant. Workflows call mcp-hub (http://mcp-hub:3003, mapped to the
# host via extra_hosts) carrying this token and their wf:<name> OBO identity.
HUB_SERVICE_TOKEN=
AGENCY_TENANT_ID=
# Event-bridge shared secret — MUST equal the platform's N8N_BRIDGE_SECRET.
N8N_BRIDGE_SECRET=
# In-app notifications: the notify flows (stale-approval-chaser, compliance-gate-nag,
# on-org-updated-notify) raise a platform in-app notification for THIS user via the hub `notify`
# tool — REQUIRED for those flows. No external channel needed.
NOTIFY_USER_ID=
# Optional external channel (Slack/Teams/webhook). NOT used by the built flows anymore (they use
# in-app notify above); kept for any custom flow you add that posts externally.
NOTIFY_WEBHOOK_URL=
# on-client-created-seed: project the seeded kickoff task lands in.
INTAKE_PROJECT_ID=
# task-sla: project whose tasks are swept for SLA breaches (single-project v1).
SLA_PROJECT_ID=
# pipeline-fanout REPORT branch (WD-06, D-4): pipeline_runs has no project_id column, so this is
# the PM project the meeting-report doc + review task land under (v1; a per-run project link is
# a documented follow-up). Reuses NOTIFY_USER_ID as the review task's assignee ("the run's PM"
# placeholder — same reasoning as the pre-existing report/scope notify targets).
WEBDEV_REPORT_PROJECT_ID=
# digest-fanout: talks to the bot admin API directly (not via the hub).
BOT_URL=
BOT_ADMIN_TOKEN=
# on-inbound-lead webhook ingest — fail-closed; set to true only when you wire it up.
INGEST_ENABLED=false
INGEST_SECRET=
# SM-56 (Standard-queue collect edge) -- NOW CONSUMED, and REQUIRED to use that edge. The platform's
# POST /api/:tenantId/modules/search/rank-pulls/callback compares this, in constant time, against the
# x-gaiada-search-callback-secret header on every request (deliberately NOT reusing N8N_BRIDGE_SECRET,
# the internal event-bridge secret: a relay carrying an EXTERNAL vendor's postback sits at a different
# trust boundary, and one shared secret would mean a compromise of either grants both).
#
# FAIL-CLOSED: while this is EMPTY the callback route refuses EVERY request (401). That is deliberate --
# "no secret configured" must never mean "skip the check". Set it (both here and in the platform's own
# env) before wiring a postback relay, and expect 401s until you do.
#
# Note SM-55's ruling still stands: automation must never TRIGGER a paid vendor pull. This edge is not
# one -- a collect retrieves a task already paid for and issues no task_post -- which is exactly why it
# is allowed to exist as a service edge while scheduled pulls moved into the platform (SM-54).
SEARCH_CALLBACK_SECRET=

# TR-11: reports-nightly-facts / reports-eod-reminder / reports-morning-escalation call
# platform-nest DIRECTLY (not through the hub) — `POST facts/recompute` and the two checkin
# service reads (pending-reminders / missed-yesterday) are deliberately NOT MCP tools (Blueprint
# §9.2). PLATFORM_SERVICE_TOKEN is the SAME shared secret the platform's own config.serviceToken /
# mcp-hub's config.platformToken already read from this exact env var name — not a new credential,
# but treat it with equal care: a direct platform call is bounded only by Cerbos + the seeded
# service account's role (company_admin via seed:automation), not by the hub's per-workflow tool
# allow-list. PLATFORM_URL defaults to the VPS-network hostname; standalone, the compose's
# extra_hosts resolves it to the host's published :3004 the same way HUB_URL/mcp-hub already does.
PLATFORM_URL=http://platform:3004
PLATFORM_SERVICE_TOKEN=

# TR-22: reports-weekly-seal / reports-monthly-seal's notify recipients — a comma-separated list
# of platform user ids (e.g. "uuid-1,uuid-2"). Same static-list shape as NOTIFY_USER_ID above, not
# a live per-department Cerbos role lookup (see automation/README.md). Leave empty to seal+render
# with zero notifications sent (still fully functional, just silent).
REPORTS_NOTIFY_USER_IDS=
